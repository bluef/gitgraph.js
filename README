gitgraph.js a tool for converting "git log --graph" from pure ascii character graph to a canvas image.

jQuery is only used as tool for extracting graph row from DOM

Check http://bluef.github.com/git-graph-canvas/ and canvas.html for demo